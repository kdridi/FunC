dist: xenial
sudo: required
language: c

before_install:
  - sudo add-apt-repository ppa:snaipewastaken/ppa
  - sudo apt-get update
  - sudo apt-get install criterion-dev

script:
  - make re
  - make test